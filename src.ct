fun main() => {
  
  for (@i = 0 to 10 step 1) {
    print("i: ");
    println(i);
  }

  println("fuori dal for");
  println("");

  if (3 < 5) {
    println("cazzo, abbiamo appena scoperto che 3 < 5");
  }
  <- 0;
}